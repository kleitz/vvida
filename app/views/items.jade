div.not-loggedin-div(ng-if='!currentUser')
  md-card.profile-body.edit-profile(layout='column' layout-align='center center')
    div.profile-pic
      img(src='images/lock.png')
    p.md-headline.packed-bottom You are currently not logged in.
    p.md-headline.packed-top Click below to login or signup  to continue using vvida.
    md-button.login-btn(class="md-raised md-primary" aria-label='Login/Signup' ng-click='login()') Go to Login/Signup Page
.items(ng-if='currentUser' layout="column")
  div(ng-cloak="")
    md-card
      md-toolbar.md-accent
        .md-toolbar-tools(aria-label="settings")
          span Add product
      md-content.md-padding
        form(method="post" enctype="multipart/data" ng-submit="addItems()")
          md-input-container
            md-icon(class="fa fa-envelope")
            md-select(placeholder="Categories" ng-model="item.catId" md-on-open="loadCategories()" class="select")
              md-option(ng-value="category.id" ng-repeat="category in categories") {{category.type}}
          md-input-container
            md-icon(class="fa fa-envelope")
            input(ng-model="item.name" type="text" placeholder="Product" ng-required="true")
          md-input-container
            md-icon(class="fa fa-envelope")
            input(ng-model="item.description" type="text" placeholder="Description" ng-required="true")

          br
          md-button.md-raised.md-warn(class="login-button" type="submit")
            i(class="fa fa-plus-circle")
            | Add
