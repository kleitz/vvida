.events(layout="row")
  div(layout="row")
    md-sidenav.md-sidenav-left.md-whiteframe-1dp(md-component-id="evcatNav" md-is-locked-open="$mdMedia(\"gt-md\")")
      md-toolbar
        h1.md-toolbar-tools(ui-sref="events.all({view:\"grid\", page: 1})") All Events
      md-content
        md-button.btn-style(md-ink-ripple ng-repeat="category in categories" ui-sref="events.categoryEvents({catId: category.id})")  {{category.name}}
        md-divider
        md-button.btn-style(md-ink-ripple hide-gt-md ng-click="close()") Close Navbar

  div(layout="column" flex)
    div(layout="row" layout-align="start center")
      md-button.md-icon-button(md-no-ink ng-click="toggleSidenav()" aria-label="Menu" hide-gt-md)
        i.fa.fa-bars
      span(hide-gt-md) Categories
      span(ng-show="recentEvents.length === 0 && popularEvents.length === 0" class="no_events_msg") There are currently no events.
    md-divider

    div(ng-if="loadEvents.length > 0")
      .eventList(layout="column" ng-if="!nextView")
        .md-padding(layout="row" layout-align="start center" ng-if="recentEvents.length > 0")
          h3.md-subheader Recent Events
          span(flex)
          md-button(ui-sref="events.all({view:\"grid\", page: 1})") See All Events

        md-divider
        div.gridItems(flex layout-gt-sm="row" layout-sm="row" layout-align="start start")
          div.md-padding(flex flex-sm="100" flex-gt-sm="33" ng-repeat="event in loadEvents | orderBy:'-time' " layout-align="center start" layout-margin)
            .images(style="background-image: url('{{event.Images[0].img_url}}');")
            div.content(layout="column")
              div.category {{event.Category.name}}
              h2.md-subhead(ui-sref="viewEvent({id: event.id})") {{event.name}}
              span {{getTime(event.time).day}} @{{getTime(event.time).time}}
            md-button.md-raised(md-ink-ripple) See Details

        md-divider
        .md-padding(layout="row" layout-align="start center")
          h3.md-subheader Popular Events
        md-divider
        div.gridItems(layout="row" layout-sm="row" layout-xs="column" layout-align="start start")
           div.md-padding(flex flex-sm="50" flex-gt-sm="33" ng-repeat="event in popularEvents" layout-align="center start" )
            .images(style="background-image: url('{{event.Images[0].img_url}}');")
            div.content(layout="column")
              div.category {{event.Category.name}}
              h2.md-subhead(ui-sref="viewEvent({id: event.id})") {{event.name}}
              span {{getTime(event.time).day}} @{{getTime(event.time).time}}
            md-button.md-raised(md-ink-ripple) See Details

      div(ui-view="inner")



