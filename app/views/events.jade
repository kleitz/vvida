span(ng-show="loadEvents.length === 0" class="no_events_msg") There are currently no events.

md-sidenav.md-sidenav-left.md-whiteframe-z2(md-component-id="eventNav")
  md-toolbar
    h1.md-toolbar-tools(ui-sref="events") Events Categories
  md-content(layout="column")
    md-button.btn-style(ng-click="setCat(list.name)" md-ink-ripple ng-repeat="list in lists" )  {{list.name}}
    md-divider
    md-button.btn-style(md-ink-ripple hide-gt-md ng-click="close()") Close

  
div.events(layout="column" layout-padding)
  div(layout="row" layout-align="start center" layout-padding)
    md-button.md-icon-button(ng-click="toggleSidenav()" aria-label="Events Menu")
      i.fa.fa-bars.fa-2x
    h2 {{eventCat || "Recent Events"}}

  md-grid-list(md-cols-xs='1' md-cols-sm='1' md-cols-md='2' md-cols-gt-md='3' md-row-height-gt-md='1:1' md-row-height='2:2' md-gutter='12px' md-gutter-gt-sm='8px')
    md-grid-tile(ng-repeat="event in loadEvents" style="background: url('{{event.Images[0].img_url}}') no-repeat; background-size: 100% 100%;")
      md-grid-tile-header
        h3 {{event.name}}
      md-grid-tile-footer
        div(flex layout="column" layout-padding)
          span.md-subheadline {{event.venue}}
          span {{parseTime(event.time).day}} 
          span {{parseTime(event.time).time}}
